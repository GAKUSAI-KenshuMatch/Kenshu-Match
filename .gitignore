# Node
node_modules/

# Next.js
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# Environment variables (Supabase keys) — never commit actual values
.env
.env.local
.env*.local
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
# Supabase CLI local state (contains pooler connection string — never commit)
supabase/.temp/
supabase/.branches/
