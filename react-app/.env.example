# Supabase project connection (anon key only — safe for the browser, data is protected by RLS).
# Copy this file to .env and fill in your project's values from the Supabase dashboard.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
